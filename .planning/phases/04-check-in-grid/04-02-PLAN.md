---
phase: 04-check-in-grid
plan: 02
type: tdd
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/types/visit.ts
  - src/lib/grid.ts
  - src/lib/grid.test.ts
autonomous: true
requirements:
  - XPLR-02

must_haves:
  truths:
    - "VisitWithCoords type exists in src/types/visit.ts with latitude and longitude on the places join"
    - "src/lib/grid.ts exports GRID_BOUNDS, CELL_LAT, CELL_LNG constants and four functions"
    - "All grid.test.ts tests pass (GREEN)"
  artifacts:
    - path: "src/types/visit.ts"
      provides: "Extended Visit type with coordinate-aware places join"
      exports: ["Visit", "VisitWithCoords"]
    - path: "src/lib/grid.ts"
      provides: "Pure grid math — no external deps, no Supabase calls"
      exports: ["GRID_BOUNDS", "CELL_LAT", "CELL_LNG", "latLngToCellKey", "cellKeyToBounds", "isInAnkaraBounds", "buildCellCounts"]
  key_links:
    - from: "src/lib/grid.ts"
      to: "src/types/visit.ts"
      via: "VisitWithCoords import for buildCellCounts parameter type"
      pattern: "import.*VisitWithCoords.*from.*@/types/visit"
    - from: "src/app/grid/GridMap.tsx"
      to: "src/lib/grid.ts"
      via: "latLngToCellKey, cellKeyToBounds, isInAnkaraBounds imports"
      pattern: "import.*from.*@/lib/grid"
---

<objective>
Extend the Visit type to include coordinates, then TDD-implement the pure grid math utilities in src/lib/grid.ts.

Purpose: The grid math is the core algorithmic unit of Phase 4. TDD ensures the coordinate-to-cell mapping is correct before any UI is built on top of it.
Output: VisitWithCoords type, grid.ts with all exported utilities, all grid.test.ts tests GREEN.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-check-in-grid/04-RESEARCH.md
@.planning/phases/04-check-in-grid/04-01-SUMMARY.md

<interfaces>
<!-- Existing types the executor should extend -->

From src/types/visit.ts (current):
```typescript
export interface Visit {
  id: string
  user_id: string
  place_id: string
  visited_at: string
  created_at: string
  places?: {
    id: string
    name: string
    slug: string
    category: string
    city?: string
  } | null
}
```

Grid math constants (validated Ankara bounding box from RESEARCH.md):
- minLat: 39.769, maxLat: 40.1007
- minLng: 32.5394, maxLng: 33.0064
- CELL_LAT: 0.004  (~444m vertical)
- CELL_LNG: 0.0047 (~400m horizontal)
- Ankara center (Kızılay): 39.9208, 32.8541
</interfaces>
</context>

<feature>
  <name>Grid math utilities (src/lib/grid.ts)</name>
  <files>src/types/visit.ts, src/lib/grid.ts, src/lib/grid.test.ts</files>
  <behavior>
    RED phase (extend grid.test.ts):
    - latLngToCellKey(39.9208, 32.8541) returns string matching /^\d+:\d+$/
    - latLngToCellKey returns row = floor((lat - 39.769) / 0.004), col = floor((lng - 32.5394) / 0.0047)
    - cellKeyToBounds('38:66') returns [[swLat, swLng], [neLat, neLng]] where all are numbers and neLat > swLat
    - isInAnkaraBounds(39.9208, 32.8541) === true
    - isInAnkaraBounds(41.0082, 28.9784) === false
    - buildCellCounts with two visits at same lat/lng → { 'row:col': 2 }
    - buildCellCounts with visit where latitude is null → that visit skipped (count stays 0)
    - buildCellCounts with visit outside Ankara bounds → that visit skipped

    GREEN phase (implement grid.ts):
    - Export GRID_BOUNDS, CELL_LAT, CELL_LNG as named constants
    - latLngToCellKey: Math.floor arithmetic, returns `${row}:${col}`
    - cellKeyToBounds: reconstruct SW and NE corners from row:col and constants
    - isInAnkaraBounds: boundary check using GRID_BOUNDS
    - buildCellCounts(visits: VisitWithCoords[]): Record<string, number>
      - Skip visits where places?.latitude == null or places?.longitude == null
      - Skip visits where !isInAnkaraBounds(lat, lng)
      - Aggregate via counts[key] = (counts[key] ?? 0) + 1
  </behavior>
  <implementation>
Step 1 — Extend src/types/visit.ts: add VisitWithCoords interface that extends Visit's places join to include latitude and longitude:
```typescript
export interface VisitWithCoords {
  id: string
  user_id: string
  place_id: string
  visited_at: string
  created_at?: string
  places?: {
    id: string
    name: string
    slug: string
    category?: string
    neighborhood?: string
    latitude: number | null
    longitude: number | null
  } | null
}
```

Step 2 — Update src/lib/grid.test.ts: add buildCellCounts tests (replace the trivial stub with real test cases). Import buildCellCounts from './grid' and test with VisitWithCoords fixtures.

Step 3 — Run tests (confirm RED on grid.ts missing).

Step 4 — Create src/lib/grid.ts with the full implementation. No external imports — pure arithmetic only.

Step 5 — Run `npx jest src/lib/grid.test.ts --no-coverage` and confirm all tests pass (GREEN).
  </implementation>
</feature>

<verification>
npx jest src/lib/grid.test.ts --no-coverage — all tests PASS
</verification>

<success_criteria>
- src/types/visit.ts exports both Visit (unchanged) and VisitWithCoords (new)
- src/lib/grid.ts exports: GRID_BOUNDS, CELL_LAT, CELL_LNG, latLngToCellKey, cellKeyToBounds, isInAnkaraBounds, buildCellCounts
- All grid.test.ts tests GREEN
- No external imports in grid.ts (pure arithmetic only)
</success_criteria>

<output>
After completion, create `.planning/phases/04-check-in-grid/04-02-SUMMARY.md`
</output>
