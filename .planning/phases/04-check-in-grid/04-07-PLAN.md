---
phase: 04-check-in-grid
plan: 07
type: execute
wave: 5
depends_on:
  - 04-06
files_modified: []
autonomous: false
requirements:
  - XPLR-01
  - XPLR-02

must_haves:
  truths:
    - "User can check in to a place and see the toast notification"
    - "After check-in, navigating to /grid shows the corresponding cell colored"
    - "Grid persists across page refresh (data from database)"
    - "Clicking a colored cell shows the place name"
    - "All automated tests pass"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for all Phase 4 (Check-in + Grid) requirements. No code changes — this plan only verifies what Plans 01-06 built.

Purpose: Toast interactions, Leaflet map rendering, and check-in flow require real-browser verification that unit tests cannot cover. This is the phase gate before marking Phase 4 complete.
Output: Verification sign-off on XPLR-01 and XPLR-02.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-check-in-grid/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite</name>
  <files></files>
  <action>
Run the full test suite to confirm no regressions before human verification:
```
npx jest --no-coverage
```
All tests must pass. Fix any failures before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>npx jest --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <done>All tests pass. No regressions in checkIns.test.ts, grid.test.ts, or any prior test files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify XPLR-01 and XPLR-02 in the running browser</name>
  <action>Human verification of both Phase 4 requirements in the running application. Run dev server with `npm run dev` and follow the verification steps below.</action>
  <what-built>
Phase 4 complete implementation:
- DB migration: places.latitude/longitude columns added, visits unique constraint dropped
- checkIn() service: INSERT-based, allows multiple visits per place
- Grid math: custom bounding-box cell math for Ankara (~83x99 cells, ~444m x 400m per cell)
- CheckInButton: in place detail hero, toast on success, auth redirect for guests
- /grid page: full-screen Leaflet map with caramel-colored visited cells and cell click panel
- Navigation: /grid linked from Navbar (authenticated) and own profile ("Haritam" button)
  </what-built>
  <how-to-verify>
Run `npm run dev` then open http://localhost:3000

**XPLR-01: Check-in to a place**
1. Log in as a test user
2. Navigate to any place detail page (e.g. /explore, click a place)
3. Verify the "Check In" button is visible in the place header alongside "Gideceğim Yerler"
4. Click "Check In" while logged in
5. Verify: "Mekana check-in yapıldı!" toast appears at the bottom of the screen
6. Verify: button changes to "Check edildi" with a checkmark icon
7. Click "Check In" again on the same place
8. Verify: second check-in succeeds (toast appears again — multiple check-ins are allowed)
9. Log out, navigate to a place page, click "Check In"
10. Verify: redirected to /auth/login

**XPLR-02: Grid map shows visited cells**
1. Log in, click "Harita" in the Navbar (or visit /grid directly)
2. Verify: Leaflet map loads, centered on Ankara with OSM tiles
3. Verify: if any previously visited places have latitude/longitude set, their cells appear as caramel-colored rectangles
   - Note: existing places may not have coordinates yet. To test: set latitude and longitude on a test place directly in Supabase (e.g. 39.9208, 32.8541 for Kızılay), then check in to it
4. Click a colored cell
5. Verify: panel slides up showing the place name(s) and visit date(s) for that cell
6. Click X to close the panel
7. Refresh the page
8. Verify: colored cells still appear (data persists from database)
9. Verify: own profile page shows "Haritam" button linking to /grid
  </how-to-verify>
  <verify>
    <automated>npx jest --passWithNoTests --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>XPLR-01 and XPLR-02 confirmed working in the browser</done>
  <resume-signal>Type "approved" if both XPLR-01 and XPLR-02 pass, or describe any issues found (include which requirement failed and what happened)</resume-signal>
</task>

</tasks>

<verification>
Manual verification per the steps above. Both XPLR requirements must pass.
</verification>

<success_criteria>
- XPLR-01: Check-in button visible in place hero; toast fires on success; button updates to "Check edildi"; second check-in succeeds; logged-out redirect to /auth/login
- XPLR-02: /grid page loads with Leaflet map; visited cells colored caramel with opacity; cell click shows place panel; data persists across refresh; Harita in Navbar and Haritam on own profile
</success_criteria>

<output>
After human approval, create `.planning/phases/04-check-in-grid/04-07-SUMMARY.md`
</output>
