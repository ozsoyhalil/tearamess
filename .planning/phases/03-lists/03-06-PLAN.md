---
phase: 03-lists
plan: "06"
type: execute
wave: 5
depends_on: ["03-05"]
files_modified: []
autonomous: false
requirements: [LIST-01, LIST-02, LIST-03, LIST-04]

must_haves:
  truths:
    - "All four LIST requirements verified by human tester in the running app"
    - "Wishlist button toggles correctly with optimistic update on place detail page"
    - "New custom list creation works end-to-end from profile Lists tab"
    - "ListItemSelector adds and removes a place from multiple lists"
    - "Private list returns 404-equivalent for non-owner; public list browsable via direct URL"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint: confirm all four LIST requirements are working correctly in the running app. No code changes — this plan only verifies what Plans 01-05 built.

Purpose: RLS policy enforcement (LIST-04) and optimistic UI flows (LIST-01, LIST-03) require real-browser verification that automated unit tests cannot cover. This is the phase gate before marking Phase 3 complete.

Output: Verification sign-off on all four LIST requirements
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-lists/03-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify complete Phase 3 Lists feature in browser</name>
  <action>Human verification of all four LIST requirements in the running application. Run dev server with `npm run dev` and follow the verification steps below.</action>
  <what-built>
Complete Phase 3 Lists feature:
- WishlistButton on place detail page (optimistic toggle, logged-out redirect)
- CreateListModal on profile Lists tab (new named list creation)
- ListItemSelector popover on place detail (add/remove place from multiple lists)
- List detail page at /lists/[id] (owner: edit name, toggle privacy, delete list, remove places; visitor: read-only)
- Profile Lists tab on own profile (all lists, wishlist pinned first)
- Profile Lists tab on other user's profile (public lists only)
  </what-built>
  <how-to-verify>
Start the dev server: `cd /Users/ibrahimozsoy/Desktop/projectpat/tearamess && npm run dev` then open http://localhost:3000

**LIST-01: Wishlist toggle**
1. Log in as a test user
2. Navigate to any place detail page (e.g. /place/some-slug)
3. Verify "Gideceğim Yerler" button appears in the place header with bookmark icon
4. Click the button — it should immediately switch to "Listede" (optimistic update)
5. Refresh the page — "Listede" state should persist (server confirmed)
6. Click again — should revert to "Gideceğim Yerler" immediately then persist after refresh
7. Log out, navigate to a place, click the wishlist button — should redirect to /auth/login

**LIST-02: Create named list**
1. Log in, go to /profile, click the "Lists" tab
2. Verify "Gideceğim Yerler" card appears (pinned first, even if no items)
3. Click "+ Yeni Liste" button — CreateListModal should open
4. Enter a list name (e.g. "En iyi kahvaltılıklar"), optionally add description, click "Oluştur"
5. Modal closes, new list card appears in the grid immediately (optimistic add)
6. Refresh — new list still visible
7. Click the new list card — navigates to /lists/[id] showing the empty list with correct name

**LIST-03: Add/remove places from lists**
1. Navigate to any place detail page
2. Verify "Listeye Ekle" button appears next to "Gideceğim Yerler" (logged-in view)
3. Click "Listeye Ekle" — popover opens showing user's lists with checkboxes
4. Check a list — checkbox toggles, place is added (optimistic)
5. Navigate to /lists/[id] for that list — place appears in the list
6. Return to place page, uncheck the list — place removed optimistically
7. Verify "Gideceğim Yerler" is also shown as a checkable item in the popover (wishlist included)
8. On the list detail page, verify "Listeden Çıkar" button removes the place from the row

**LIST-04: Privacy toggle and public lists**
1. Create a new list on own profile Lists tab
2. Navigate to /lists/[id] for the new list
3. Verify privacy toggle appears in the header (showing "Herkese Açık")
4. Toggle to "Gizli" — list becomes private (optimistic UI update)
5. Open a private browsing window (logged out or different account)
6. Navigate to the same /lists/[id] URL — verify "Bu liste bulunamadı" or 404-equivalent
7. Toggle back to "Herkese Açık"
8. In the private window — navigate to /lists/[id] again — list should now be visible (read-only)
9. Navigate to the list owner's public profile (/users/[username]) — verify public list appears in Lists tab
10. Verify "Gideceğim Yerler" has NO privacy toggle (it is always public — no toggle visible)
  </how-to-verify>
  <verify>
    <automated>cd /Users/ibrahimozsoy/Desktop/projectpat/tearamess && npx jest --passWithNoTests --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>All four LIST requirements confirmed working in the browser</done>
  <resume-signal>Type "approved" if all four requirements pass, or describe any issues found (include which LIST-0X requirement failed and what happened)</resume-signal>
</task>

</tasks>

<verification>
Manual verification per the steps above. All four LIST requirements must pass.
</verification>

<success_criteria>
- LIST-01: Wishlist toggle works with optimistic update, persists after refresh, redirects logged-out users
- LIST-02: Named list creation via modal works end-to-end, new list appears in grid
- LIST-03: ListItemSelector popover adds/removes places from multiple lists; Listeden Çıkar works on detail page
- LIST-04: Privacy toggle changes list visibility; private list inaccessible to non-owners; public lists visible on other user's profile; Gideceğim Yerler always public (no toggle)
</success_criteria>

<output>
After human approval, create `.planning/phases/03-lists/03-06-SUMMARY.md`
</output>
