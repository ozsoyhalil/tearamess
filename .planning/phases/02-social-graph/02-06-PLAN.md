---
phase: 02-social-graph
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified: []
autonomous: false
requirements: [SOCL-01, SOCL-02, SOCL-03, SOCL-04]

must_haves:
  truths:
    - "User can follow another user and the follow button updates immediately (optimistic)"
    - "User can see follower and following counts on their own profile and click to open the list modal"
    - "Home feed shows activity cards from followed accounts with infinite scroll"
    - "Logged-out users at / see the landing page, not the feed"
    - "Public profile at /users/[username] renders with Visits tab showing visit history"
  artifacts: []
  key_links: []
---

<objective>
Wave 5: Human verification of the complete social graph feature across all four requirements.

Purpose: Automated tests cover service logic but cannot verify the follow button feel, the infinite scroll experience, or the auth-split landing page. Human eyes needed.
Output: Confirmed social graph works end-to-end in the browser.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-social-graph/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify complete social graph in browser</name>
  <action>Human verification of all four SOCL requirements in the running application. Run dev server with `npm run dev` and follow the verification steps below.</action>
  <what-built>
    Complete Phase 2 Social Graph:
    - One-way follow system with optimistic FollowButton (Takip Et / Takip Ediliyor / Takibi Bırak)
    - Follower/following counts on profiles with modal overlay lists
    - Home feed at / showing activity from followed accounts with infinite scroll
    - Auth-split /: landing page for logged-out, feed for logged-in
    - Public profile at /users/[username] with Visits|Lists|Reviews tabs
  </what-built>
  <how-to-verify>
    Run the dev server: `cd /Users/ibrahimozsoy/Desktop/projectpat/tearamess && npm run dev`

    Then verify each requirement:

    **SOCL-01 — Follow/unfollow:**
    1. Log in as User A
    2. Navigate to /users/[any-username] for a different user
    3. Click "Takip Et" — button should immediately show "Takip Ediliyor" (optimistic)
    4. Reload the page — follow state should persist (server confirmed)
    5. Hover over "Takip Ediliyor" — should reveal "Takibi Bırak"
    6. Click "Takibi Bırak" — button returns to "Takip Et"

    **SOCL-02 — Follower/following lists:**
    1. Go to /profile (own profile)
    2. Verify follower and following counts appear in the profile header
    3. Click the follower count — modal should open with list of followers
    4. Each follower entry should show avatar + name + follow/unfollow button
    5. Close modal (click backdrop or X button)

    **SOCL-03 — Activity feed:**
    1. Ensure you follow at least one account that has reviews or visits
    2. Visit / (home) while logged in — feed should show activity cards
    3. Each card shows author avatar, place name, and relative timestamp ("X saat önce")
    4. Review cards show star rating and content snippet
    5. Visit cards show just "gitti" — no rating
    6. Scroll to bottom of feed — more cards should load automatically (if 20+ items exist)
    7. Log out → visit / → verify landing page with "Giriş Yap" / "Kayıt Ol" buttons (not feed)

    **SOCL-04 — Public profile:**
    1. Navigate to /users/[username] for another user
    2. Profile header shows: avatar, display name, @username, follower/following counts, Follow button
    3. Click "Ziyaretler" tab — shows list of places they visited
    4. Click "Listeler" tab — shows placeholder (Phase 3 not yet built)
    5. Try /users/doesnotexist — should show "Kullanıcı bulunamadı" error state

    **DB prerequisite check:**
    - If any feature fails with "column does not exist" or "relation does not exist" errors, the follows or visits Supabase tables may need to be created manually via the Supabase SQL editor using the SQL from RESEARCH.md Pattern 1 and Pattern 2.
  </how-to-verify>
  <verify>
    <automated>cd /Users/ibrahimozsoy/Desktop/projectpat/tearamess && npx jest --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>All four SOCL requirements confirmed working in the browser</done>
  <resume-signal>Type "approved" if all four requirements work in the browser, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human verified: all four SOCL requirements observable and working in the browser.
</verification>

<success_criteria>
- SOCL-01: Follow/unfollow works with optimistic UI that reverts on failure
- SOCL-02: Follower/following counts visible on profile; list modal opens on count click
- SOCL-03: Feed shows visits and reviews from followed accounts; empty state with Turkish message; infinite scroll loads more
- SOCL-04: /users/[username] shows public profile with visit history tab
- Logged-out home route shows landing page, not feed
</success_criteria>

<output>
After completion, create `.planning/phases/02-social-graph/02-06-SUMMARY.md`
</output>
